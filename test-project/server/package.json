{
  "name": "test-project-server",
  "type": "module",
  "scripts": {
    "start": "run-p --print-label start:**",
    "start:build": "tsc --project tsconfig-build.json --watch --preserveWatchOutput",
    "start:server": "node --watch --enable-source-maps build/server.js",
    "build": "tsc --project tsconfig-build.json"
  },
  "dependencies": {
    "@protobuf-ts/runtime": "^2.9.4",
    "@sanjo/test-project-shared": "file:../shared",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "npm-run-all": "^4.1.5"
  }
}
